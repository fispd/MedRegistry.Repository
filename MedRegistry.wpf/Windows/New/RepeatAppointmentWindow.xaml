<Window x:Class="MedRegistryApp.wpf.Windows.New.RepeatAppointmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Повторный приём" Width="420" Height="600"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5" ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Border Background="White" CornerRadius="15" Padding="25">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Direction="315" ShadowDepth="3" Opacity="0.15" BlurRadius="10"/>
            </Border.Effect>
            
            <StackPanel>
                <!-- Заголовок -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,20" HorizontalAlignment="Center">
                    <TextBlock Text="🔁" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Повторный приём" FontSize="20" FontWeight="Bold" Foreground="#0B7FC7" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Информация о пациенте -->
                <Border Background="#E8F6F3" CornerRadius="10" Padding="15" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="👤 Пациент:" FontWeight="SemiBold" Foreground="#27AE60" FontSize="13"/>
                        <TextBlock x:Name="PatientNameText" FontSize="15" FontWeight="Bold" Foreground="#2C3E50" Margin="0,5,0,0"/>
                        <TextBlock x:Name="PatientPhoneText" FontSize="12" Foreground="#7F8C8D" Margin="0,3,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Дата -->
                <TextBlock Text="Выберите дату" FontWeight="SemiBold" FontSize="13" Foreground="#555" Margin="0,0,0,5"/>
                <DatePicker x:Name="AppointmentDatePicker" 
                            Height="35" FontSize="13"
                            SelectedDateChanged="DatePicker_SelectedDateChanged"/>

                <!-- Время -->
                <TextBlock Text="Выберите время" FontWeight="SemiBold" FontSize="13" Foreground="#555" Margin="0,15,0,5"/>
                <ComboBox x:Name="TimeComboBox" Height="35" Padding="10,0" FontSize="13" 
                          SelectionChanged="TimeComboBox_SelectionChanged"/>

                <!-- Статус доступности -->
                <TextBlock x:Name="AvailabilityText" 
                           Text="" FontSize="12" FontWeight="SemiBold"
                           Margin="0,8,0,0" HorizontalAlignment="Center"/>

                <!-- Причина -->
                <TextBlock Text="Причина приёма" FontWeight="SemiBold" FontSize="13" Foreground="#555" Margin="0,15,0,5"/>
                <ComboBox x:Name="ReasonComboBox" Height="35" Padding="10,0" FontSize="13"/>

                <!-- Кнопки -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
                    <Button Content="Отмена" 
                            Width="120" Height="40" Margin="0,0,10,0"
                            Click="Cancel_Click" FontSize="13" FontWeight="SemiBold"
                            Style="{StaticResource MenuButtonStyle}" Cursor="Hand"/>
                    <Button Content="✅ Записать" 
                            Width="130" Height="40"
                            Click="Save_Click" FontSize="13" FontWeight="SemiBold"
                            Background="#27AE60" Foreground="White"
                            Style="{StaticResource 2ButtonStyle}" Cursor="Hand"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
