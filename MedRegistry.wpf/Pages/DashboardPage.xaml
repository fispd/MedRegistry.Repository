<Page x:Class="MedRegistryApp.wpf.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MedRegistryApp.wpf.Pages"
      mc:Ignorable="d"
      Background="#F0F2F5">

    <ScrollViewer>
        <StackPanel Margin="20">
            <!-- Заголовок -->
            <TextBlock Text="🏠 Главная" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#0B7FC7" 
                       Margin="0,0,0,20"/>

            <!-- Блок статистики -->
            <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="3" Opacity="0.2" BlurRadius="5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="📊 Статистика на сегодня" 
                               FontSize="18" FontWeight="Bold" 
                               Foreground="#0B7FC7" 
                               Margin="0,0,0,15"/>
                    <WrapPanel x:Name="StatsPanel" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <!-- Ближайшие приёмы -->
            <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="3" Opacity="0.2" BlurRadius="5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="📅 Ближайшие приёмы" 
                               FontSize="18" FontWeight="Bold" 
                               Foreground="#27AE60" 
                               Margin="0,0,0,15"/>
                    <WrapPanel x:Name="AppointmentsWrapPanel" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
