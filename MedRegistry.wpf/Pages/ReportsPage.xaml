<Page x:Class="MedRegistryApp.wpf.Pages.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MedRegistryApp.wpf.Pages"
      mc:Ignorable="d" 
      Background="#F0F2F5">

    <ScrollViewer>
        <StackPanel Margin="20">
            <!-- Заголовок -->
            <TextBlock Text="📊 Отчёты и аналитика" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#0B7FC7" 
                       Margin="0,0,0,20"/>

            <!-- Общая статистика -->
            <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="3" Opacity="0.2" BlurRadius="5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="📈 Общая статистика" 
                               FontSize="18" FontWeight="Bold" 
                               Foreground="#27AE60" 
                               Margin="0,0,0,15"/>
                    <WrapPanel x:Name="GeneralStatsPanel" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>

            <!-- Статистика врачей по специализациям -->
            <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="3" Opacity="0.2" BlurRadius="5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="👨‍⚕️ Врачи по специализациям" 
                               FontSize="18" FontWeight="Bold" 
                               Foreground="#9B59B6" 
                               Margin="0,0,0,15"/>
                    <ItemsControl x:Name="DoctorStatsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8F9FA" CornerRadius="8" Padding="15" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Specialization}" 
                                                       FontSize="14" FontWeight="SemiBold" 
                                                       Foreground="#333"/>
                                            <ProgressBar Value="{Binding Count}" Maximum="20" 
                                                         Height="8" Margin="0,8,0,0"
                                                         Foreground="#3498DB"/>
                                        </StackPanel>
                                        <Border Grid.Column="1" Background="#3498DB" 
                                                CornerRadius="15" Padding="12,5" 
                                                VerticalAlignment="Center" Margin="15,0,0,0">
                                            <TextBlock FontSize="14" FontWeight="Bold" Foreground="White">
                                                <Run Text="{Binding Count}"/>
                                                <Run Text=" врачей"/>
                                            </TextBlock>
                                        </Border>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Статистика по статусам приёмов -->
            <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20"
                    BorderBrush="#E0E0E0" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="3" Opacity="0.2" BlurRadius="5"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="📅 Приёмы по статусам" 
                               FontSize="18" FontWeight="Bold" 
                               Foreground="#E67E22" 
                               Margin="0,0,0,15"/>
                    <WrapPanel x:Name="AppointmentStatsPanel" HorizontalAlignment="Left"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
