<Page x:Class="MedRegistryApp.wpf.Pages.AppointmentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MedRegistryApp.wpf.Pages"
      mc:Ignorable="d" 
      Background="#F0F2F5"
      Title="Управление приёмами">

    <ScrollViewer>
        <StackPanel Margin="20">
            <!-- Заголовок -->
            <TextBlock Text="Записи на приём" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#0B7FC7" 
                       Margin="0,0,0,15"/>

            <!-- Кнопка добавления -->
            <Button x:Name="AddAppointmentButton" 
                    Content="➕ Добавить запись" 
                    Height="40" Width="200" 
                    Background="#0B7FC7" Foreground="White" 
                    FontWeight="SemiBold"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,15"
                    Click="AddAppointment_Click" 
                    Style="{StaticResource 2ButtonStyle}"/>

            <!-- Основной контент: карточки записей -->
            <WrapPanel x:Name="AppointmentsWrapPanel" HorizontalAlignment="Left"/>
        </StackPanel>
    </ScrollViewer>
</Page>
